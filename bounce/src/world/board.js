import Background from '../view/background';

class Board {
  constructor() {
    this.view = new Background({ backgroundColor: 'lightblue' });
  }

  update() {}
  draw() {}
}

export default Board;
