const Board = () => {
  let element = document.body;

  // initialize ----------------------------------------------------------------

  element.style.backgroundColor = 'lightblue';

  // public --------------------------------------------------------------------

  const update = () => {};

  const draw = () => {};

  return { update, draw };
};

export default Board;
